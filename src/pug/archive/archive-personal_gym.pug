| <?php get_header(); ?>

main#main.l-main.mw800.margin-t20a

  section.archive-hero.archive-hero--personal
    .archive-hero__inner
      h1.archive-title 続かない人はパーソナルへ
      p.archive-lead
        | トレハジのSTEP3は、「ひとりでは続かない」と感じた人のためのパーソナルジム。<br>
        | 横浜・千葉エリアを中心に、料金や通いやすさ、雰囲気などをくらべて選べるようにまとめています。
    .archive-hero__image
      img(
        src!= "<?php echo get_template_directory_uri(); ?>/assets/img/common/personal-gym-top.webp"
        alt="パーソナルジムのイメージ"
      )

  section.archive-list
    .archive-list__inner
      | <?php if ( have_posts() ) : ?>
      .archive-cards
        | <?php while ( have_posts() ) : the_post(); ?>
        article.archive-card
          a.archive-card__link(href!= "<?php the_permalink(); ?>")
            .archive-card__thumb
              | <?php if ( has_post_thumbnail() ) : ?>
              | <?php the_post_thumbnail('medium_large'); ?>
              | <?php else : ?>
              img(
                src!= "<?php echo get_template_directory_uri(); ?>/assets/img/noimg.png"
                alt=""
              )
              | <?php endif; ?>
            .archive-card__body
              h2.archive-card__title
                | <?php the_title(); ?>
              p.archive-card__meta
                | <?php echo get_the_date('Y.m.d'); ?>
        | <?php endwhile; ?>

      .margin-t20
        | <?php get_template_part('functions/pagination'); ?>

      | <?php else : ?>
      p.archive-empty
        | 現在、パーソナルジムの情報を整理中です。<br>
        | 横浜・千葉エリアから順に、通いやすさ・料金・特徴を比較できる記事を公開していきます。
      | <?php endif; ?>

| <?php get_footer(); ?>
